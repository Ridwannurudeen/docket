# September 9 submission and assisted-demo closeout

This note supersedes earlier instructions to file the Google form and older descriptions
of the local v3-08 manual attempts. It documents outcomes without changing experiment
events, frozen inputs, registered slots, timestamps, or scoring rules.

## Google form

On September 9, the owner reported that they had completed and submitted the Google
form. The Google Forms confirmation page was subsequently inspected and states,
“Your response has been recorded.” Do not create another entry. The page exposes neither
submitted answers nor an edit-response link. Field values, selected tracks, and organizer
acceptance have not been independently verified. These corrected repository notes are not evidence that the
existing Google response contains the same wording. Any amendment must use the existing
entry's permitted edit route or organizer guidance, with owner approval.

## Local Yield outcomes

The authoritative operator ledger is `docket/advantage/v3/runs/v3-08-yield-router.jsonl`.
At this review it contains ten events and has SHA-256
`51f878e2028456fa8f57b98e97e6e070cc8d4bebc27ac31e4d84da78a6a1647b`.
It remains a local, untracked artifact; this note does not claim it has been published.

| Case | Recorded outcome | Timing and answer boundary |
|---|---|---|
| `yield-01-9474e972` | Interrupted | Detected September 9 at 06:29:41.121504 UTC. Finish time and duration are unknown. Original events remain intact; the slot is consumed. |
| `yield-02-916f992d` | Timed out | Began September 9 at 06:40:21.553381 UTC; terminated at 07:00:23.445193 UTC. `raw_output` is null and `eligible_for_speed` is false. The slot is consumed. |
| Remaining manual case | Unstarted at review | No new claim or timer was created by the assisted demonstration. |

The required three complete valid pairs were not obtained. Neither the remaining case
nor a later demonstration can replace these consumed attempts. No successful registered
human-versus-agent speed claim is made. This is an incomplete family, not a newly scored
falsifier verdict. Local operator evidence and the deployed public report are distinct
until an explicitly reviewed publication reconciles them.

## Separate assisted demonstration

The owner reviewed the supplied assistant-assisted analysis for `yield-02-916f992d` and
clicked Submit in a separate untimed local page. The downloaded record was prepared at
`2026-09-09T07:31:54.139Z`. Its filename is
`docket-assisted-demo-yield-02-916f992d.json`; its SHA-256 is
`ce2f019eb21acae90d68a9a1b51c6fabb204a347658dbfb5eff238bbd1c04915`.
The second downloaded copy is byte-identical, not another trial.

The record explicitly sets `mode` to `assisted-demonstration`, `registered` to false,
`assistant_assisted` to true, `eligible_for_speed` to false, and `elapsed_seconds` to null.
It preserves the original supplied text and the owner's reviewed sections. It is a
local text-review record, not a registered answer envelope, correctness score, unaided
human result, or evidence of organizer acceptance. The actual record remains local;
no raw answers or private Google-form fields are published by this note.

The browser-control failure was repaired before this demonstration. That repair did
not restart either registered attempt or subtract the interruption from its record.

## Corrections that must accompany the materials

- TermiX acceptance is unconfirmed. The v1 liquidity artifact explicitly says its
  manual-tool arm was performed by an agent driving Chrome, with excluded timing gaps
  and prior-information contamination. Three paired records alone do not establish
  three unaided human baselines or guaranteed sponsor eligibility.
- The runtime observed at 07:36:41 UTC on September 9 was
  `971f4f666d0969b83a6f1f33c453d8d5b0796a68`; public status was `ok`, with 144/144
  probes in its reported 24-hour window. This is a dated observation, not an uptime
  guarantee. The historical release tag is not the current runtime revision.
- The assisted-demo JSON is not a demo video. No new real-voice recording or video URL
  was supplied in this closeout. Use [the recording plan](demo-script.md) if a video is
  desired; do not label stored evidence as live execution. The owner clarified that the
  video is optional, so recording is not an outstanding submission requirement.

## Publication and remaining review

The owner approved publishing these documentation corrections on September 9.
This publication documents the outcome without adding operator artifacts to the public
experiment dataset or changing the deployed runtime.
If the already-submitted entry contains unsupported claims, prepare an amendment for the
owner using the existing entry; do not submit a duplicate or contact organizers automatically.
Retain the downloaded assisted record and the original operator ledger separately.
No new registered case, paid call, score, deployment, or form submission is authorized
by this closeout alone.
