/* Entry module for /providers. One import and one call: the page's behaviour lives in
   ../providers.js, and this file exists so the HTML names exactly one script. */

import { init } from "../providers.js?v=13";

init();
