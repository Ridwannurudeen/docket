/* Entry module for /search. One import and one call: the page's behaviour lives in
   ../search.js, and this file exists so the HTML names exactly one script. */

import { init } from "../search.js?v=13";

init();
